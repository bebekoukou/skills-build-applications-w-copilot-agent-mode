# Environment example for octofit-tracker backend

# Port the backend listens on (default 8000)
PORT=8000

# MongoDB connection string (default uses local MongoDB)
MONGODB_URI=mongodb://localhost:27017/octofit
